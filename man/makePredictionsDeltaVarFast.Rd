% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bycatchFunctions.R
\name{makePredictionsDeltaVarFast}
\alias{makePredictionsDeltaVarFast}
\title{Generate predicted total bycatch with standard errors and confidence intervals
of predictions (computationally efficient version with
help from GitHub copilot)}
\usage{
makePredictionsDeltaVarFast(
  modfit1,
  modfit2 = NULL,
  newdat,
  modtype,
  obsdatval,
  includeObsCatch,
  predictionGroups = NULL,
  CIval,
  printOutput = TRUE,
  catchType,
  common,
  shortName,
  dirname,
  run,
  modelScenario,
  startYear
)
}
\arguments{
\item{modfit1}{Fitted model object (binomial component for delta models, otherwise full model)}

\item{modfit2}{Fitted positive model object (Delta-Lognormal or Delta-Gamma only); NULL otherwise}

\item{newdat}{Prediction data frame (logbook data)}

\item{modtype}{Character string naming the model type}

\item{obsdatval}{Observer data frame (for includeObsCatch)}

\item{includeObsCatch}{Logical; whether to add observed catch to predictions}

\item{predictionGroups}{Character vector of stratification variable names}

\item{CIval}{Numeric; alpha level for confidence intervals (e.g. 0.05)}

\item{printOutput}{Logical; whether to write CSV outputs}

\item{catchType}{Value}

\item{common}{Value}

\item{shortName}{Value}

\item{dirname}{Value}

\item{run}{Value}

\item{modelScenario}{Value}

\item{startYear}{Value}
}
\description{
Avoids forming the n x n observation-level covariance matrix A \%\emph{\% Sigma \%}\% t(A)
by instead computing the equivalent scalar variance as
crossprod(v, Sigma_beta \%\emph{\% v) where v = t(A) \%}\% deriv is a p-length vector
(p = number of model parameters). This reduces memory from O(n^2) to O(np)
and computation from O(n^2 p) to O(np + p^2), making it feasible for large n.
}
\details{
For delta models (Delta-Lognormal, Delta-Gamma), the delta method is applied
to both component models jointly,with observation-level residual variance
computed from the Lo et al. (1992) variance-of-a-product formula.
}
\keyword{internal}
