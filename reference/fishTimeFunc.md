# Function to calculate number of hours fished in each day counted as first set to last haul

Function to calculate number of hours fished in each day counted as
first set to last haul

fishTimeFunc

## Usage

``` r
fishTimeFunc(timeout, timein, prop.sampled = 1)

fishTimeFunc(timeout, timein, prop.sampled = 1)
```
