# Generate standard errors of predictions from simulation from regression coefficients and their var/covar matrix

Generate standard errors of predictions from simulation from regression
coefficients and their var/covar matrix

## Usage

``` r
getSimSE(modfit, df1, transFunc = "none", offsetval = NULL, nsim)
```

## Arguments

- modfit:

  Value

- df1:

  Value

- transFunc:

  Value

- offsetval:

  Value

- nsim:

  Value
