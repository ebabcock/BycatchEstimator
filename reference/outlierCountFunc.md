# Function to count outliers, defined as more than numSD standard deviations from the mean.

Function to count outliers, defined as more than numSD standard
deviations from the mean.

## Usage

``` r
outlierCountFunc(x, numSD = 8)
```

## Arguments

- x:

  Value

- numSD:

  Value
